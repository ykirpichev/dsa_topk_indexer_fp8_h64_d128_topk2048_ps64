[solution]
name = "my-team-solution-v1"      # Solution name
definition = "dsa_topk_indexer_fp8_h64_d128_topk2048_ps64"
author = "team-name"              # Team/author name

[build]
language = "triton"               # triton | cuda
entry_point = "kernel.py::kernel" # file::function (BuildSpec format)
destination_passing_style = false # value-returning (topk_indices tuple)
# Pure PyTorch path: no extra pip deps (see branch pytorch-bmm)
dependencies = []
