[solution]
name = "my-team-solution-v1"      # Primary: Triton + torch.bmm (fast on B200)
definition = "dsa_topk_indexer_fp8_h64_d128_topk2048_ps64"
author = "team-name"

[build]
language = "triton"
entry_point = "kernel.py::kernel"

# Python / B200 FP8 UMMA (blackwell_fp8_batched_mm.py): set language = "python",
# entry_point = "kernel.py::kernel", dependencies = ["nvidia-cutlass-dsl"]
