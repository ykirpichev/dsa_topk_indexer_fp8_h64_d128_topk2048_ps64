[solution]
name = "my-team-solution-v1"      # Primary: Triton + torch.bmm (fast on B200)
definition = "dsa_topk_indexer_fp8_h64_d128_topk2048_ps64"
author = "team-name"

[build]
language = "triton"
entry_point = "kernel.py::kernel"

# FP8 TMMA path (Modal: FIB_MODAL_DSA_FP8_TMMA_MM=1): language = "python",
# entry_point = "kernel.py::kernel", dependencies = ["nvidia-cutlass-dsl", "triton"]
